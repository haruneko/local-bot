use_default_settings: true

general:
  instance_name: "local-bot-search"

search:
  safe_search: 0
  formats:
    - html
    - json

server:
  secret_key: "local-bot-searxng-secret"
  bind_address: "0.0.0.0"
  port: 8080
