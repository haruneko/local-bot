services:
  searxng:
    image: searxng/searxng:latest
    ports:
      - "8080:8080"
    environment:
      - SEARXNG_SECRET=local-bot-searxng-secret
    volumes:
      - ./config/searxng:/etc/searxng:rw
    restart: unless-stopped
